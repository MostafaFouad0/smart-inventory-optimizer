const VERSION = "/api";
const AUTH = `${VERSION}/auth`;
const STAFF = `${VERSION}/staff`;
const ADMIN = `${VERSION}/admin`;
const DASHBOARD = `${VERSION}/dashboard`;
const PRODUCTS_REVENUES = `${VERSION}/products-revenues`;
const PRODUCTS_STOCK = `${VERSION}/products-stock`;
module.exports = {
  AUTH,
  STAFF,
  ADMIN,
  PRODUCTS_REVENUES,
  DASHBOARD,
  PRODUCTS_STOCK,
};
