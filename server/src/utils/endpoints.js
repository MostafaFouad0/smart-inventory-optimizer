const VERSION = "/api";
const AUTH = `${VERSION}/auth`;
const STAFF = `${VERSION}/staff`;
const ADMIN = `${VERSION}/admin`;
const FORGETPASSWORD = `${VERSION}/forgetPassword`;


module.exports = { AUTH, STAFF, ADMIN , FORGETPASSWORD };